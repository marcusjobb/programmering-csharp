---
title: Operatorer
description: "Operatorer i Ordlista — C#-boken av Marcus Ackre Medina"
layout: default
parent: Ordlista
nav_order: 25
---
## Operatorer

| Ord | Förklaring |
| --- | --- |
| Addition `+` | Lägger ihop två tal — eller sätter ihop strängar (konkatenering). |
| Division `/` | Delar ett tal med ett annat. Heltalsdivision (`int / int`) kastar bort decimaldelen. |
| Inkrement `++` / dekrement `--` | Ökar eller minskar en variabel med exakt 1. |
| Jämförelseoperatorer `== != < > <= >=` | Jämför två värden och returnerar alltid `true` eller `false`. |
| Logiska operatorer `&& \|\| !` | Kombinerar eller vänder på booleska uttryck — `&&` (och), `\|\|` (eller), `!` (inte). |
| Modulo `%` | Ger resten efter heltalsdivision, t.ex. `10 % 3` ger `1`. |
| Multiplikation `*` | Multiplicerar två tal. |
| Operatorsordning | Multiplikation och division beräknas före addition och subtraktion, om inte parenteser säger annat. |
| Sammansatta tilldelningsoperatorer `+=` `-=` `*=` `/=` | Genväg för att ändra en variabel baserat på sitt eget värde, t.ex. `poäng += 10`. |
| Short-circuit evaluation | C# slutar utvärdera ett logiskt uttryck så fort resultatet redan är avgjort. |
| Subtraktion `-` | Drar av ett tal från ett annat. |
